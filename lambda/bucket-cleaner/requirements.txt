# AWS DRS Orchestration - Bucket Cleaner Lambda Requirements
# This Lambda function uses only AWS SDK (boto3) which is provided by the Lambda runtime
# No additional dependencies required

# boto3 is provided by AWS Lambda runtime
# cfnresponse is provided by AWS Lambda runtime for custom resources