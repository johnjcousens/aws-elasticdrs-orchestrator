# Development and CI/CD Tools
# Install with: pip install -r requirements-dev.txt
#
# Additional tools (not pip-installable):
#   cfn_nag:    gem install cfn-nag          (Ruby gem for CloudFormation security)
#   shellcheck: brew install shellcheck      (Shell script linter)
#   grype:      brew install grype           (CVE/vulnerability scanner)
#   syft:       brew install syft            (SBOM generator)
#   npm audit:  comes with Node.js           (JavaScript dependency audit)

# CloudFormation validation
cfn-lint==1.43.4

# Python code quality
flake8==7.1.1
black==24.10.0
isort==5.13.2

# Security scanning
bandit==1.9.2
detect-secrets==1.5.0
safety==3.3.1
checkov==3.2.334
semgrep==1.102.0

# Testing
pytest==8.3.4
pytest-cov==6.0.0
pytest-mock==3.14.0
pytest-asyncio==0.24.0
moto==5.0.26
hypothesis==6.122.3

# AWS SDK (already in Lambda runtime, but needed for local testing)
boto3==1.35.94
botocore==1.35.94
