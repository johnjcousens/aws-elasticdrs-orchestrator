# AWS Configuration for Frontend Build
# Copy this file to .env.test and populate with your actual values
# This file is used by frontend/build.sh to generate aws-config.json

# AWS Region where your resources are deployed
COGNITO_REGION=us-east-1

# Cognito User Pool ID (find in CloudFormation outputs or Cognito console)
# Format: us-east-1_XXXXXXXXX
COGNITO_USER_POOL_ID=

# Cognito App Client ID (find in CloudFormation outputs or Cognito console)  
# Format: alphanumeric string
COGNITO_CLIENT_ID=

# API Gateway Endpoint URL (find in CloudFormation outputs or API Gateway console)
# Format: https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod
API_ENDPOINT=

# Instructions:
# 1. Copy this file: cp .env.test.template .env.test
# 2. Get values from CloudFormation stack outputs:
#    aws cloudformation describe-stacks --stack-name aws-drs-orchestrator-test --region us-east-1 --query 'Stacks[0].Outputs'
# 3. Populate the values above
# 4. Run: ./scripts/sync-to-deployment-bucket.sh --build-frontend --deploy-cfn
