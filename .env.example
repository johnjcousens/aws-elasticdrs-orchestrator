# AWS DRS Orchestration Platform - Environment Configuration Example
# Copy this file to .env.dev or .env.test and fill in your actual values

# ============================================================================
# AWS Account Configuration
# ============================================================================

# Orchestration Account (where the platform is deployed)
ORCHESTRATION_ACCOUNT_ID=123456789012

# Target Account (where DR recovery instances will be launched)
TARGET_ACCOUNT_ID=987654321098

# Staging Account (optional - for DRS staging area)
STAGING_ACCOUNT_ID=111111111111

# ============================================================================
# AWS Region Configuration
# ============================================================================

AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# ============================================================================
# Environment
# ============================================================================

ENVIRONMENT=dev  # Options: dev, test, staging, prod

# ============================================================================
# DRS Configuration
# ============================================================================

# External ID for cross-account role assumption
EXTERNAL_ID=DRSOrchestration2024

# ============================================================================
# Deployment Configuration
# ============================================================================

# S3 Deployment Bucket (for CloudFormation templates and Lambda packages)
DEPLOYMENT_BUCKET=aws-drs-orchestration-${ENVIRONMENT}

# Project Name
PROJECT_NAME=aws-drs-orchestration

# ============================================================================
# Optional: Custom Role ARN
# ============================================================================

# If using an external orchestration role (e.g., HRP integration)
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/CustomOrchestrationRole

# ============================================================================
# Notes
# ============================================================================

# 1. Never commit .env files with real credentials to version control
# 2. Use AWS IAM roles and profiles for authentication when possible
# 3. Replace example account IDs (123456789012, etc.) with your actual IDs
# 4. Ensure cross-account roles are properly configured before deployment
